# Thesis Evaluation Repository

Reproducibility package for [thesis title], evaluating AllDifferent propagator
variants (V0–V3) within the Circuit constraint in Pumpkin (LCG solver).

## Structure
- `solver/` — Pumpkin solver variants (baseline + V0-V3), see SOLVER_VERSIONS.md
- `experiments/` — benchmark configs, run scripts, DelftBlue SLURM jobs
- `analysis/` — notebooks and result data/plots
